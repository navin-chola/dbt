version: 2

# 🟢 1. Define sources here
sources:
  - name: raw
    database: dbt_db           # Replace with your actual Snowflake database
    schema: raw                # Replace with your actual schema name
    tables:
      - name: customers

# 🟢 2. Define model descriptions and tests
models:
  - name: stg_customers
    description: "Stage the raw customer data"
    columns:
      - name: email
        tests:
          - not_null

  - name: mart_customers
    description: "Final cleaned and joined customer data"
